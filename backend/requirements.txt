# Web framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0

# Settings & validation (Pydantic v2)
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0

# HTTP client — used by the FastAPI TestClient now and by real providers in M6.
httpx>=0.27,<1.0

# AI client — Gemini query understanding (M3).
google-genai>=1.0,<2.0
