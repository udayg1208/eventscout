import { RecommendationsPage } from "@/views/RecommendationsPage";

export const metadata = { title: "Recommended For You — EventScout" };

export default function Page() {
  return <RecommendationsPage />;
}
