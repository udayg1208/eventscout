import { EntityIndexPage } from "@/views/EntityIndexPage";

export const metadata = { title: "Communities — EventScout" };

export default function Page() {
  return <EntityIndexPage kind="communities" />;
}
